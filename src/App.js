import './App.css';
import { BeanieCalculator } from './BeanieCalculator';

export const App = () => {
  return <div className='centerText'>days since i was promised a beanie:
  < BeanieCalculator />
  </div>
}
